[package]
name = "tkern_derive_utils"
version = "0.0.0"
edition = "2021"
license = "MIT OR Apache-2.0"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = { version = "1.0.40", default-features = false }
quote = { version = "1.0.20", default-features = false }
syn = { version = "=1.0.98", features = ["extra-traits"], default-features = false }
